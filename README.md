# XLY Package

XLY 是一个 Flutter 工具包，提供了一些常用的功能和部件。虽然目前仍在开发中，但已经可以在项目中使用一些基本功能。

## 功能

当前，这个包提供了以下功能：

1. Toast 消息显示
2. 导航辅助函数
3. 自定义按钮组件
4. 应用初始化辅助

## 安装

要使用 XLY 包，首先在你的 `pubspec.yaml` 文件中添加 `xly` 依赖：

## 示例

```dart
toast('这是一条测试Toast消息');
```
