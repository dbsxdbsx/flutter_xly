# .issue/

暂缓但未丢失的问题现场。AGENTS.md **Active Context** 只链接 `items/` 下 `suspended` / `blocked` 条目。

- 新建：在 `items/` 下用 `snake_case` 文件名 + frontmatter（`status`、`updated`）。
- 已闭环：移至 `items/_archive/` 或删除，并更新 AGENTS.md。

当前无开放条目。
