import 'package:flutter_test/flutter_test.dart';

void main() {
  test('sanity: true is true', () {
    // Arrange
    const value = true;
    // Act
    // (no-op)
    // Assert
    expect(value, isTrue);
  });
}
